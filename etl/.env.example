# API Key de Google Maps (Geocoding API)
GOOGLE_MAPS_API_KEY=tu_api_key_aqui

# Conexión a PostgreSQL (el contenedor Docker expone el puerto 5432 al host)
DATABASE_URL=postgresql://saferoutes:saferoutes_dev@localhost:5432/saferoutes
